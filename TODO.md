# TODO

All tasks below implement ADR-0018 § Revision 2026-08-17 (`adr/0018-shape-indexed-signal-types.md`).
Read that section before starting any task — it has the full rationale and the final name table.

- [ ] CE-001: Rename the narrow single-value shape from `Signal` to `Cell`
  **Skill:** cause-effect-dev
  **Context:** In `src/nodes/signal.ts`: rename types `Signal<T>` → `Cell<T>`, `MutableSignal<T>` → `MutableCell<T>`. Narrow the `Symbol.toStringTag` field from `?: string` to `?: 'Cell'` (closes the structural leak in ADR-0018 Revision Problem 1 — do this for `Cell`/`List`/`Store` alike, not just `Cell`, so no shape is structurally assignable to another via an unliteraled tag). Add a *new*, separate `Signal<T>` type as the umbrella — structural, `{ readonly [Symbol.toStringTag]?: string; get(): T }` (this is the type the old `Signal<T>` used to be, kept verbatim, just no longer meaning "the narrow shape"). Update `TYPE_SIGNAL` in `src/graph.ts` to `TYPE_CELL = 'Cell'` (rename the constant and its exported name; check all three call sites: `graph.ts:499`, `graph.ts:550`, plus the export). Rename `isSignal`/`isMutableSignal` → `isCell`/`isMutableCell`, same tag-plus-`get()` / tag-plus-`get()`-plus-`set()` logic, now checking `'Cell'`. Do not add an umbrella guard — ADR-0018 defers that; the umbrella is checked structurally only.

- [ ] CE-002: Realign the `create*`/`derive*` factories
  **Skill:** cause-effect-dev
  **Context:** Depends on CE-001. In `src/nodes/signal.ts` (or wherever the renamed module lands): `createSignal` → `createCell`, with `createState` retained as an exported alias (same function, second export name — `createState` already exists in `src/nodes/state.ts`, keep it, just have it return `MutableCell<T>`). `deriveSignal` → `deriveCell` (same dispatch logic — sync fn / async fn / seed+`watched` — just renamed and now returns `Cell<T>`). Add `deriveComputed(fn, options?)` as the sync-only narrow factory, replacing `createMemo`'s public export (the internal recompute logic in `src/nodes/memo.ts` is unaffected — only the exported factory name and its `create*`→`derive*` prefix change, since it returns a readonly type). Remove `createTask` and `createSensor` from the public export surface — their internal node-construction logic in `src/nodes/task.ts`/`src/nodes/sensor.ts` stays as-is and becomes something `deriveCell` dispatches to internally for async-function and seed+`watched` inputs respectively (mirror how `deriveSignal` already dispatched to them before this change — same dispatch, just no longer also exported as standalone factories).

- [ ] CE-003: Propagate the Cell/umbrella-Signal distinction through composite and integration files
  **Skill:** cause-effect-dev
  **Context:** Depends on CE-001, CE-002. Update `src/nodes/list.ts`, `src/nodes/store.ts`, `src/nodes/slot.ts`, `src/nodes/effect.ts` to import `Cell`/`MutableCell` wherever the old import meant the narrow single-value shape (e.g. `list.ts`'s `ItemSignalOptions<T, S extends Signal<T>>` with default `S extends MutableSignal<T> = MutableSignal<T>` — items are single values, so this becomes `S extends Cell<T> = MutableCell<T>`), and keep importing `Signal` only where the umbrella is genuinely meant (e.g. `effect.ts`'s `MatchHandlers<T extends readonly Signal<...>[]>` — effects can match against any shape, so this stays `Signal`). `src/nodes/slot.ts`'s JSDoc line "Slots are not MutableSignals" becomes "Slots are not MutableCells" — check whether Slot should now also be described as excluded from the `Signal` umbrella statement, or only from `MutableCell`; ADR-0018 doesn't change Slot's design, only the vocabulary referring to it. Leave `src/errors.ts`'s `NullishSignalValueError`, `UnsetSignalValueError`, `ReadonlySignalError`, `validateSignalValue` **unchanged** — per ADR-0018 Revision, these are correctly umbrella-scoped now, not narrow-scoped; do not rename them to `*CellValueError`.

- [ ] CE-004: Update the v1→v2 codemod for the second naming flip
  **Skill:** cause-effect-dev
  **Context:** Depends on CE-001, CE-002. `tools/codemod-v2.ts` currently rewrites v1.x names toward `Signal`/`MutableSignal`/`isSignal`/`createSignal`/`deriveSignal` and emits a warning about the `isSignal`/`isMutableSignal` 2.0 narrowing (around lines 281, 285). Update it to target the final names instead: `Cell`/`MutableCell`/`isCell`/`isMutableCell`/`createCell`/`deriveCell`/`deriveComputed`, and add rewrite rules for anyone who already adopted the pre-revision `Signal`-narrow names on this branch (`createMemo`→`deriveComputed`, `createTask`/`createSensor` call sites → flag for manual migration to `deriveCell`, since there's no mechanical 1:1 rewrite — the call needs its arguments reshaped to `deriveCell`'s dispatch signature). Update `test/codemod-v2.test.ts` to match.

- [ ] CE-005: Update and rename test suites
  **Skill:** cause-effect-dev
  **Context:** Depends on CE-001–CE-004. Rename `test/signal.test.ts` → `test/cell.test.ts` and update its assertions/imports for the new names. Update `test/state.test.ts`, `test/memo.test.ts` (tests `deriveComputed` now, consider renaming to `test/computed.test.ts`), `test/task.test.ts`, `test/sensor.test.ts` (the latter two now test internal dispatch via `deriveCell` rather than a standalone public factory — adjust test entry points accordingly, keep the files if the internal behavior still warrants dedicated coverage). Rewrite `test/v2-transition.test.ts` explicitly — it documents the 1.x→2.0 naming transition and now needs a second transition documented, not a mechanical find-replace. Update `test/list.test.ts`, `test/store.test.ts`, `test/slot.test.ts`, `test/util.test.ts`, `test/untrack.test.ts`, `test/collection.test.ts`, `test/derive-list.test.ts`, `test/effect.test.ts`, `test/guides.test.ts` for any `Signal`/`MutableSignal` references that should now read `Cell`/`MutableCell` vs. genuinely-umbrella `Signal`.

- [ ] CE-006: Sync `CONTEXT.md` vocabulary
  **Skill:** tech-writer
  **Context:** Depends on CE-001–CE-005 landing. Rewrite the **Signal** entry to describe the umbrella (structural, `.get()`-only membership, no guard); rewrite/add a **Cell** entry for the narrow single-value shape (created by `createCell()`/`createState()`, or `deriveCell()`/`deriveComputed()`; narrowed by `isCell()`); update **MutableSignal** → **MutableCell**. Remove `atom`, `cell` from the **Signal** entry's `_Avoid_` list (Cell is no longer a synonym to avoid — it's the term). Check the **Node** entry's `_Avoid_` list (currently includes `cell`) — this now collides with the public `Cell` type name; resolve the conflict (Node is internal-only, Cell is public, so they can coexist, but the avoid-list wording needs to make that distinction explicit rather than banning "cell" outright). Update the Construction comparison table and any `isSignal`/`createSignal`/`deriveSignal`/`createMemo`/`createTask`/`createSensor` mentions throughout.

- [ ] CE-007: Sync `AGENTS.md` quick-reference
  **Skill:** tech-writer
  **Context:** Depends on CE-006. Update the "Construction Routing" table and the guards bullet list to the final names (`createCell`/`createState`, `deriveCell`, `deriveComputed`, `isCell`, `isMutableCell`). Update the "Narrow single-origin factories... exist for tree-shaking" bullet — it currently lists four (`createState`, `createMemo`, `createTask`, `createSensor`); now only two survive (`createState`/`createCell`, `deriveComputed`).

- [ ] CE-008: Sync `README.md`, `GUIDE.md`, `REQUIREMENTS.md`, `ARCHITECTURE.md`
  **Skill:** tech-writer
  **Context:** Depends on CE-006. Update all narrow-identifier mentions (`Signal`/`MutableSignal`/`isSignal`/`createSignal`/`deriveSignal`/`createMemo`/`createTask`/`createSensor`) to the ADR-0018-Revision names throughout these four documents. `V2_REFACTORING_ANALYSIS.md` also has 50 references — confirm with the user whether that file is still live working material or should be left as a historical record (it predates this revision and describes the original, since-amended decision).

- [ ] CE-009: Write a new `MIGRATION-2.0.md` section for the second naming flip
  **Skill:** tech-writer
  **Context:** Depends on CE-006. `MIGRATION-2.0.md` already has a section documenting the first `isSignal`/`isMutableSignal` meaning-flip (1.x umbrella → 2.0 narrow, around lines 53–70). Add a **new, separate section** for the second flip (2.0 narrow → 2.0 umbrella again, narrow renamed to `Cell`) — do not edit the existing section, since it's still accurate for anyone who saw the intermediate state on this branch. Cover: `Signal`/`MutableSignal` now mean the umbrella again; `Cell`/`MutableCell` are the new narrow names; `isSignal`/`isMutableSignal` are retired (not narrowed further — gone); `isCell`/`isMutableCell` are their replacements; `createMemo` → `deriveComputed`; `createTask`/`createSensor` are removed with no direct replacement (route to `deriveCell`, with a worked example for each since the call shape changes).

- [ ] CE-010: Update `CHANGELOG.md`
  **Skill:** changelog-keeper
  **Context:** Depends on CE-001–CE-005 landing. The existing `2.0.0-next` entries describe the original ADR-0018 decision (the `Signal`-narrow naming) — leave that history alone and add new entries under the current unreleased heading for the Revision 2026-08-17 changes: `Cell`/`MutableCell` replace `Signal`/`MutableSignal` as the narrow shape, `Signal` becomes the umbrella, `deriveComputed` replaces `createMemo`, `createTask`/`createSensor` are removed as public factories in favor of `deriveCell`.
