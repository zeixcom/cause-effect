# TODO

All tasks below implement ADR-0018 (`adr/0018-shape-indexed-signal-types.md`, revised 2026-08-17).
Read it before starting any task — it has the full rationale and the final name table.

- [x] CE-001: Rename the narrow single-value shape from `Signal` to `Cell` — reviewed ✓
  **Skill:** cause-effect-dev
  **Changed:** `src/nodes/signal.ts` → renamed to `src/nodes/cell.ts`; `src/graph.ts` (`TYPE_SIGNAL` → `TYPE_CELL`, 3 call sites + export).
  **Review:** Approved. `Cell`/`MutableCell` narrow types, restored umbrella `Signal<T>`, and `isCell`/`isMutableCell`/`isSignal`/`isMutableSignal` guards all match ADR-0018 §1 and §5 exactly. Verified independently: `bunx tsc --noEmit`, `bunx biome check .`, and `bun test` (661 pass / 0 fail) all clean.

- [x] CE-002: Realign the `create*`/`derive*` factories — reviewed ✓
  **Skill:** cause-effect-dev
  **Changed:** `src/nodes/cell.ts` (`createCell`, `deriveCell`), `src/nodes/memo.ts` (`createMemo` → `deriveComputed`), `index.ts` (export surface).
  **Review:** Approved. Matches ADR-0018 §5's two-narrow-entry-points table exactly. `createTask`/`createSensor` correctly dropped from `index.ts` while their internal node-construction logic in `task.ts`/`sensor.ts` is untouched and still reachable through `deriveCell`'s dispatch.

- [x] CE-003: Propagate the Cell/umbrella-Signal distinction through composite and integration files — reviewed ✓, with a follow-up (CE-011)
  **Skill:** cause-effect-dev
  **Changed:** `src/nodes/list.ts`, `src/nodes/store.ts`, `src/nodes/slot.ts`, `src/nodes/effect.ts`.
  **Review:** Approved overall — `store.ts`'s primitive-property/nested-composite split, `slot.ts`'s JSDoc update, `effect.ts`'s retained umbrella `Signal` for `MatchHandlers`, and `errors.ts` left untouched are all correct. One issue found in `list.ts`: narrowing `ItemSignalOptions<T, S extends Signal<T>>` to `S extends Cell<T>` (per this task's own instructions) also narrows `createList`'s/`deriveList`'s `createItem` customization point, which previously accepted a `Store`- or `List`-shaped item factory — a deliberate, tested feature (commit `516668d`, "fix: Improve type inference for custom item factories"), not the accidental structural leak ADR-0018 Revision Problem 1 targets. Two tests (`test/list.test.ts`, `test/collection.test.ts`) exercised this with `createItem: createStore` and needed rewriting to Cell-shaped item factories to keep compiling — meaning the capability is now silently gone with no ADR/REQUIREMENTS record of the removal. See CE-011.

- [x] CE-011: Restore custom composite item factories for `List`'s `createItem` — reviewed ✓
  **Skill:** cause-effect-dev
  **Changed:** `src/nodes/list.ts` (`ItemSignalOptions`, `ListOptions`, `List`, `MutableList`, `DeriveListOptions`, `ExternalListOptions`, `listFacade`, `isMutableItem`, `createExternalList`, `deriveList`, `createList`, `isList`, `isMutableList`); `test/list.test.ts`, `test/collection.test.ts`, `test/v2-transition.test.ts`.
  **How:** Loosened `S`'s bound from `Cell<T>`/`MutableCell<T>` back to the umbrella `Signal<T>` / `Signal<T> & { set(value: T): void }` everywhere listed above, keeping every default parameter unchanged at `Cell<T>`/`MutableCell<T>` — so the common (no-`createItem`) case is exactly as tight as CE-003 left it, and only the explicit-`createItem` case regains `Store`/`List` eligibility. Reverted the two CE-003-adapted tests back to `createItem: createStore`, restoring the original nested-Store-field regression coverage and the original custom-factory type-inference assertion.
  **Found and fixed en route:** a single broadened generic signature for `createList` regressed a *different*, ordinary case — `const x: List<T> = createList([...])` (any contextual annotation, no `createItem`) failed to compile, because TS's generic-default resolution picked the loosened bound instead of `MutableCell<T>` when a contextual type was also in play. An overload split (no-`createItem` vs. with-`createItem`) fixed that but broke inference for a bare generic function passed as `createItem` (e.g. `createItem: createStore` without an explicit `<T>`) in the other direction — overload resolution weakens higher-order generic inference in a way a single signature doesn't. Kept the single signature (it's the one that supports bare `createStore` correctly) and instead added an explicit type argument at the one real call site that needed it (`test/v2-transition.test.ts`). Documented the trade-off in `createList`'s JSDoc so a future caller hitting the same contextual-annotation case knows to add `<T>` rather than treating it as a bug.
  **Check:** Confirmed independently reproducible before the fix and clean after: `bunx tsc --noEmit`, `bunx biome check .`, `bun test` (661 pass / 0 fail), `bun run build` all clean; bundle sizes unchanged (index.js 52.56 KB, core gzip 2080B). Worth double-checking whether the same contextual-annotation footgun exists elsewhere in the type surface (e.g. `Store`'s or a future shape's generically-bounded customization points) before it bites a real consumer.
  **Correction (post-review):** the user caught a follow-on imprecision in the first pass — `DeriveListOptions`, `ExternalListOptions`, `createExternalList`, and `deriveList`'s external-push overload all defaulted `S` to `Cell<T>`, but their actual runtime item factory (when `createItem` is omitted) is `createState`, which returns `MutableCell<T>` — a mismatch that in fact predates this task entirely (confirmed present under the old `Signal`-narrow naming before any ADR-0018 Revision work started). Corrected those four defaults to `MutableCell<T>`. Left `List<T,S>`'s own bare default and `isList`'s alone at `Cell<T>` — that one has to stay the loosest covariant bound, since a genuinely read-only List (`deriveList(fn)`, items built from `deriveComputed`) must still satisfy bare `List<T>`/`ListSource<T>`; defaulting it to `MutableCell<T>` would have broken that. Two internal pass-through casts in `deriveList`'s sync/async-derivation branches needed a documented `as unknown as` bridge afterward, since `keyedAdapter`/`derivePerItem` never read `options.createItem` on that path and the value doesn't need to satisfy the now-stricter default. Re-verified clean: `tsc`, `biome`, `bun test` (661/661), `bun run build`.
  **Review:** Approved. The umbrella `Signal<T>` bound at the `createItem` customization point with unchanged `Cell`/`MutableCell` defaults is the right split — the common case stays as tight as CE-003 left it, and only the explicit-opt-in case regains `Store`/`List` eligibility. `List<T,S>`'s bare `Cell<T>` default correctly stays the loosest covariant bound. Both `as unknown as` bridges verified safe from the code: neither `keyedAdapter` nor `derivePerItem` reads `options.createItem` on the sync/async-fn paths, and no public overload exposes `S` on the per-item path. The handoff's open question is closed: `createList`/`deriveList` are the only public APIs with a generic `S` default parameter — `createStore`/`deriveStore` have no generic customization point (`StoreOptions` is `{ watched }` only) — so the contextual-annotation footgun is confined to `List`, where it is documented in JSDoc. All claims independently reproduced: `bunx tsc --noEmit`, `bunx biome check .`, `bun test` (661 pass / 0 fail), `bun run build` (index.js 52.56 KB), working tree clean afterward. Minor doc-precision follow-up: CE-012.

- [x] CE-005: Update and rename test suites — reviewed ✓
  **Skill:** cause-effect-dev
  **Changed:** `test/signal.test.ts` → `test/cell.test.ts`, `test/memo.test.ts` → `test/computed.test.ts`, plus updates across ~20 test files and `bench/reactivity.bench.ts`.
  **Review:** Approved. `test/v2-transition.test.ts` documents the second transition (Cell/umbrella-Signal) as a new section rather than a mechanical find-replace, as instructed. `test/task.test.ts`/`test/sensor.test.ts` correctly test `createTask`/`createSensor` at their internal module path now that they're not public, while still covering the public `deriveCell` dispatch path. `test/regression-performance.test.ts` was correctly bridged across the `createMemo`/`deriveComputed` rename (compares against a pinned older published release that predates it) rather than left broken. Depends on CE-011's resolution for full closure — the two tests it adapted should be revisited per that task's outcome.

- [x] CE-004: Update the v1→v2 codemod for the second naming flip — reviewed ✓
  **Skill:** cause-effect-dev
  **Changed:** `tools/codemod-v2.ts` (header doc table, `RENAMES`, new `FLAGGED_CALLS`, `MutableSignal` import flag, `syncImports` candidate/stale lists, report hints); `test/codemod-v2.test.ts` (rewritten, 24 tests).
  **How:** The task's premise was partly stale — the codemod never rewrote toward `Signal`/`createSignal`/`deriveSignal`; it had composite renames plus an `isSignal`-narrowing warning. Actual changes: (1) `Collection`→`DerivedList` targeted a bridge name that no longer exists in v2 — retargeted to `Collection`→`List`, and added the 1.5.0 bridge names `DerivedList`→`List`, `isDerivedList`→`isList`, `DerivedStore`→`Store` (all verified against the `v1.5.0` tag's export surface; there is no `isDerivedStore` in 1.5.0). (2) Single-value renames added: `createMemo`/`createComputed`→`deriveComputed`, `createMutableSignal`→`createCell`, `deriveSignal`→`deriveCell`. (3) `createSignal` is population-ambiguous (1.x shape-sniffing vs. the branch's narrow factory) → flagged, never rewritten. (4) The `isSignal`/`isMutableSignal` narrowing warning was **removed** — the umbrella meaning survives into 2.0 (the 1.5.0 narrow interlude is corrected and ships in 1.5.1, per user decision recorded under CE-012). (5) Origin guards (`isState`/`isMemo`/`isTask`/`isSensor`/`isComputed`) are now flagged — `MIGRATION-2.0.md` already promised this but the codemod never did it. (6) Options/callback unification renames: `ComputedOptions`/`SensorOptions`→`DeriveSignalOptions`, `SensorCallback`→`SignalCallback`. (7) A still-used `type MutableSignal` import is flagged (import-precise, own-type declarations of that name are not) — the name has no v2 export and both 1.4 and 1.5 populations need a decision. `createTask`/`createSensor` call sites are flagged with the `deriveCell` migration shape per the task instructions.
  **Check:** `bun test` 669 pass / 0 fail (codemod file 16→24 tests), `bunx tsc --noEmit` and `bunx biome check .` clean. Note for CE-009: `MIGRATION-2.0.md`'s "Running the codemod" section still describes the old behavior (claims the codemod flags `isSignal` sites and suggests narrowing to `DerivedList`) — must be updated when the second-flip section is written.
  **Review:** Approved. All rewrite targets verified against the `v1.5.0` tag's actual export surface; the `isSignal`/`isMutableSignal` warning removal matches the 1.5.1 correction; flagging (never rewriting) population-ambiguous `createSignal` and argument-reshaping `createTask`/`createSensor` is the right call. One accepted caveat: `createMemo`/`createComputed` call sites using the 1.x `value` option compile with a wrong key after the rename to `deriveComputed` (expects `initial`) — TypeScript's excess-property check makes this loud, and `MIGRATION-2.0.md` documents the `value`→`initial` change. Sequencing note: the options/callback renames (`ComputedOptions`/`SensorOptions`→`DeriveSignalOptions`, `SensorCallback`→`SignalCallback`) emit names that CE-013 retires again — CE-013 must update the codemod's targets in the same change.

- [x] CE-006: Sync `CONTEXT.md` vocabulary
  **Skill:** tech-writer
  **Context:** Depends on CE-001–CE-005 landing. Rewrite the **Signal** entry to describe the umbrella (structural, `.get()`-only membership, guarded by `isSignal()`/`isMutableSignal()` — restored v1.x meaning, not removed); rewrite/add a **Cell** entry for the narrow single-value shape (created by `createCell()`/`createState()`, or `deriveCell()`/`deriveComputed()`; narrowed by `isCell()`/`isMutableCell()`). Update **MutableSignal** → describe as the umbrella's mutable counterpart (still exists, `isMutableSignal()` guard), distinct from the new **MutableCell**. Remove `atom`, `cell` from the **Signal** entry's `_Avoid_` list (Cell is no longer a synonym to avoid — it's the term). Check the **Node** entry's `_Avoid_` list (currently includes `cell`) — this now collides with the public `Cell` type name; resolve the conflict (Node is internal-only, Cell is public, so they can coexist, but the avoid-list wording needs to make that distinction explicit rather than banning "cell" outright). Update the Construction comparison table and the guards list — both `isSignal`/`isMutableSignal` (umbrella) and `isCell`/`isMutableCell` (narrow) are current, not one replacing the other — plus any `createSignal`/`deriveSignal`/`createMemo`/`createTask`/`createSensor` mentions throughout.

- [x] CE-007: Sync `AGENTS.md` quick-reference
  **Skill:** tech-writer
  **Context:** Depends on CE-006. Update the "Construction Routing" table and the guards bullet list to the final names (`createCell`/`createState`, `deriveCell`, `deriveComputed`, `isCell`, `isMutableCell`). Update the "Narrow single-origin factories... exist for tree-shaking" bullet — it currently lists four (`createState`, `createMemo`, `createTask`, `createSensor`); now only two survive (`createState`/`createCell`, `deriveComputed`).

- [x] CE-008: Sync `README.md`, `GUIDE.md`, `REQUIREMENTS.md`, `ARCHITECTURE.md`
  **Skill:** tech-writer
  **Context:** Depends on CE-006. Update all narrow-identifier mentions (`Signal`/`MutableSignal`/`createSignal`/`deriveSignal`/`createMemo`/`createTask`/`createSensor` used to mean the *narrow single-value shape*) to the ADR-0018-Revision names (`Cell`/`MutableCell`/`createCell`/`deriveCell`/`deriveComputed`) throughout these four documents. `isSignal`/`isMutableSignal` mentions are **not** part of this rename — per ADR-0018 §5 they keep their v1.x umbrella meaning; only add `isCell`/`isMutableCell` alongside them as the new narrow guards, don't replace them. `V2_REFACTORING_ANALYSIS.md` also has 50 references — confirm with the user whether that file is still live working material or should be left as a historical record (it predates this revision and describes the original, since-amended decision). **User decision (2026-08-17):** left as a historical record, untouched.

- [x] CE-009: Write a new `MIGRATION-2.0.md` section for the second naming flip
  **Skill:** tech-writer
  **Context:** Depends on CE-006. `MIGRATION-2.0.md` already has a section documenting the first `isSignal`/`isMutableSignal` meaning-flip (1.x umbrella → 2.0 narrow, around lines 53–70). Add a **new, separate section** for the second flip (2.0 narrow → 2.0 umbrella again, narrow renamed to `Cell`) — do not edit the existing section, since it's still accurate for anyone who saw the intermediate state on this branch. Cover: `Signal`/`MutableSignal` now mean the umbrella again; `Cell`/`MutableCell` are the new narrow names; `isSignal`/`isMutableSignal` flip back to their v1.x umbrella meaning (structural, any shape) rather than being retired — anyone who adopted the intermediate 2.0-narrow behavior on this branch must switch those specific call sites to `isCell`/`isMutableCell`, but the `isSignal`/`isMutableSignal` names themselves keep working, just with a different (wider) match; `createMemo` → `deriveComputed`; `createTask`/`createSensor` are removed with no direct replacement (route to `deriveCell`, with a worked example for each since the call shape changes).

- [x] CE-010: Update `CHANGELOG.md`
  **Skill:** changelog-keeper
  **Context:** Depends on CE-001–CE-005 landing. The existing `1.5.0` entries describe the original ADR-0018 decision (the `Signal`-narrow naming) — leave that history alone and add new entries under the current unreleased heading for the Revision 2026-08-17 changes: `Cell`/`MutableCell` replace `Signal`/`MutableSignal` as the narrow shape, `Signal` becomes the umbrella, `deriveComputed` replaces `createMemo`, `createTask`/`createSensor` are removed as public factories in favor of `deriveCell`.

- [x] CE-012: Fix stale ADR anchors and one JSDoc table row from the ADR-0018 Revision work
  **Skill:** tech-writer
  **Context:** Follow-up to CE-011 (found during review of `e4408f6`). (a) `src/nodes/list.ts:69` and the "Type Inference for custom createItem" comments in `test/list.test.ts` and `test/collection.test.ts` cite "ADR-0018 Revision Problem 1" — a heading that the ADR compaction (e6c9557) removed. Re-anchor those comments to §1's tag-literal paragraph ("a `List` is never structurally assignable to `Cell<T>` merely by having `get()`"), which is where that rationale now lives. (b) `deriveCell`'s JSDoc origin table (`src/nodes/cell.ts`, the `| sync function | — | Synchronous derivation | deriveComputed |` row) puts `deriveComputed` in the "Replaces" column — wrong direction: per ADR-0018 §3 the replaced factory is `createMemo`, and `deriveComputed` survives as the sync-only narrow alternative (§5). Fix the row to `createMemo` (or relabel the column if the narrow-alternative reading was intended). (c) *Resolved by the Architect:* the `@since` convention is **renames and other breaking changes get `2.0.0`; only non-breaking signature changes keep their original date**. Applied: `createCell` 0.9.6→2.0.0, `deriveComputed` 0.18.0→2.0.0 (renames), `isStore` 0.15.0→2.0.0 (narrowed type flipped to the readonly base in 2.0, matching `isList`'s existing 2.0.0), and `isMutableStore` 2.0.0→1.5.0 (shipped as this exact guard in the v1.5.0 back-port, verified against the tag). Everything else checked out: `isPending`/`abort`/`isMutableList`/`deriveList`/`deriveStore` 1.5.0 and the untouched 1.x names keep their dates. **User decision (2026-08-17):** the 1.5.0 narrow `Signal`/`MutableSignal` and tag-based `isSignal`/`isMutableSignal` were a bug, not a version boundary — the corrected umbrella meaning ships backported in v1.5.1, so those four keep their original `@since` dates (0.9.0/0.15.2/0.9.0/0.15.2) and are exempt from the breaking→2.0.0 rule.

- [x] CE-013: Rename the Cell family's options and watched-callback types (`SignalOptions`→`CellOptions`, `DeriveSignalOptions`→`DeriveCellOptions`, `SignalCallback`→`CellCallback`) — reviewed ✓
  **Skill:** cause-effect-dev
  **Context:** Completes the shape-indexed naming of the Cell family's auxiliary types, per ADR-0018's taxonomy (user decision 2026-08-17; `CellCallback` chosen over role-named `WatchedCallback`/`EmitCallback` to keep the `(Cell|List|Store)Callback` trio and the `(Derive)?(Cell|List|Store)Options` family uniformly shape-indexed). `DeriveSignalOptions` is used only by the single-value derive family (`deriveCell`/`deriveComputed`/`createTask`), `SignalOptions` only by the single-value create family (`createState`/`createCell`/`createSensor`, `Slot`) — under umbrella-`Signal` both names are misleading. Changes: (1) `src/graph.ts` — rename the three type definitions and their JSDoc, the `SignalCallback` reference in `DeriveSignalOptions`'s doc comment (≈line 136), the export block (≈lines 899, 925–926); that doc comment also still names the pre-Revision `deriveSignal`/`createMemo` — update to `deriveCell`/`deriveComputed`. (2) Update imports/references in `src/nodes/cell.ts`, `state.ts`, `memo.ts`, `task.ts`, `sensor.ts`, `slot.ts`. (3) `index.ts` re-export names. (4) Regenerate `types/` via `bun run build`. (5) `tools/codemod-v2.ts` — retarget the RENAMES emitted by CE-004 (`ComputedOptions`/`SensorOptions`→`DeriveCellOptions`, `SensorCallback`→`CellCallback`), the header table, and the `syncImports` candidate list; update `test/codemod-v2.test.ts` expectations. No other test/bench/doc files reference these names (verified by grep; the two `MIGRATION-2.0.md` mentions are CE-009's scope). Type-only renames — zero runtime/bundle impact. All three types currently carry no `@since`; add `@since 2.0.0` to each while renaming (`SignalOptions` and `DeriveSignalOptions` shipped as those exact names in v1.5.0 — renamed, so breaking per the CE-012 rule; `SignalCallback`/`CellCallback` never shipped — brand-new name). Verify with `bun test`, `bunx tsc --noEmit`, `bunx biome check .`, `bun run build`.
  **Changed:** `src/graph.ts` (three type definitions, JSDoc, `@since 2.0.0` each, export block), `src/nodes/cell.ts`, `state.ts`, `memo.ts`, `task.ts`, `sensor.ts` (incl. its `SignalCallback` re-export), `slot.ts`, `index.ts`; `tools/codemod-v2.ts` + `test/codemod-v2.test.ts`; `types/` regenerated.
  **How:** Exact-token renames, longest first (`DeriveSignalOptions` before `SignalOptions`, which it contains as a substring). JSDoc refreshed to final vocabulary while renaming — `DeriveCellOptions`'s doc no longer cites the pre-Revision `deriveSignal`/`createMemo`/`createSensor`, and `CellOptions`'s doc now names its actual consumers (`createState`/`createCell`, `createSlot`). Import blocks re-alphabetized. Codemod: retargeted the CE-004 rules to the final names, and added three new rename sources discovered during implementation — `DeriveSignalOptions`→`DeriveCellOptions` and `SignalOptions`→`CellOptions` both shipped in v1.5.0 (verified against the tag; member-identical to today's shapes, so fully meaning-preserving) and would otherwise be compile errors against v2; `SignalCallback`→`CellCallback` is branch-adopter-only (never shipped in 1.x), same treatment as `createMemo`. `syncImports` candidates now `DeriveCellOptions`/`CellOptions`/`CellCallback`. Deliberately not renamed: `ItemSignalOptions` in `list.ts` — module-private, and its "Signal" is the umbrella sense (per CE-011 an item signal may be Cell-, Store-, or List-shaped), so the name is correct under the final taxonomy.
  **Check:** `bun test` 670 pass / 0 fail (one new codemod test for the bridge-name renames), `bunx tsc --noEmit` and `bunx biome check .` clean, `bun run build` regenerates `types/`; `index.js` byte-identical at 52.56 KB — confirming a type-only change with zero runtime/bundle impact.
  **Review:** Approved. Full diff read: all rename sites including the `deriveCell` dispatch casts and `sensor.ts`'s re-export are consistent; `tsc` passing is itself proof no stale reference survives (`graph.ts` no longer exports the old names). JSDoc was refreshed rather than find-replaced — stale pre-Revision `deriveSignal`/`createMemo`/`createSensor` mentions are gone, and the three `@since 2.0.0` tags follow the CE-012 rule. The codemod extension (three new rename sources) is correct and necessary: `SignalOptions`/`DeriveSignalOptions` shipped in v1.5.0 and would be compile errors against v2 without rules. Leaving `ItemSignalOptions` alone is right — umbrella sense, module-private.

- [x] CE-014: Sync the agent skills themselves (SKILL.md files, references, workflows) to the Cell taxonomy
  **Skill:** tech-writer
  **Context:** Depends on CE-006–CE-009 and CE-012 (use the landed `CONTEXT.md`/`AGENTS.md` vocabulary as the anchor). The `.agents/skills/` tree still teaches the pre-Revision API — grep found stale identifiers in 12 files. By priority: (1) `cause-effect/references/signal-types.md` is built around the retired surface (`deriveSignal` ×11, `createSignal` ×6, narrow `Signal`/`MutableSignal`, `isSignal`/`isMutableSignal` presented as narrow guards) — rewrite to `Cell`/`MutableCell`, `createCell`/`deriveCell`, umbrella guards; (2) the shared references loaded by *both* the dev and consumer skills — `api-facts.md`, `non-obvious-behaviors.md`, `error-classes.md` — still cite `createSignal`/`deriveSignal`/`createMemo`/`createTask`/`createSensor`/narrow `MutableSignal` as current API facts; (3) `cause-effect-dev/references/source-map.md` — file→API map needs `createMemo`→`deriveComputed` and `createTask`/`createSensor` re-marked as internal-only (reachable through `deriveCell`'s dispatch), plus the now-retired origin guards `isState`/`isMemo`/`isTask`/`isSensor` dropped or re-anchored; (4) `cause-effect/SKILL.md` and `cause-effect/workflows/use-api.md` quick-reference/examples; (5) incidental format-example mentions (the `todo_format` example in `architect/SKILL.md`, `changelog-keeper/SKILL.md`, `tech-writer` references `document-map.md`/`ste100-style.md`/`tone-guide.md`) — update for consistency, not load-bearing. Same guardrails as CE-006–CE-008: `isSignal`/`isMutableSignal` keep their restored umbrella meaning (don't swap them for `isCell`), `createState`/`deriveComputed` still exist, and umbrella-sense `Signal` wording (e.g. `ItemSignalOptions`) is correct as-is. Verify with a final grep that no retired name (`deriveSignal`, `createMemo`, public-sense `createTask`/`createSensor`/`createSignal`) remains asserted as current API anywhere under `.agents/skills/`.
